const SPEED = 1;

export default class GameState extends Phaser.State {
  init() {
    console.log(`init`);
  }
  preload() {
    console.log(`preload`);
  }

  create() {
    console.log(`create`);
  }

  update() {
  }

  render() {
  }
}
