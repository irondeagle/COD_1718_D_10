const SPEED = 1;

export default class GameState extends Phaser.State {

  init() {
    console.log(`this is the init function`);
  }

  preload() {
    console.log(`preload`);
  }

  create() {
    console.log(`create`);
  }

  update() {
    console.log(`update`);
  }

  render() {
  }
}
